import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context";

/** BODY module — see docs/plan. Register all routes of this module here. */
export async function registerBodyModule(_app: FastifyInstance, _ctx: AppContext) {
  // implemented by the owning backend agent
}
