import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context";

/** NUTRITION module — see docs/plan. Register all routes of this module here. */
export async function registerNutritionModule(_app: FastifyInstance, _ctx: AppContext) {
  // implemented by the owning backend agent
}
