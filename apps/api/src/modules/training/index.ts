import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context";

/** TRAINING module — see docs/plan. Register all routes of this module here. */
export async function registerTrainingModule(_app: FastifyInstance, _ctx: AppContext) {
  // implemented by the owning backend agent
}
