import { ProgramEditorScreen } from "../../src/features/training";

/** Program create / edit, full screen with live muscle volume. Route: `/(modals)/program-editor`. */
export default function ProgramEditorRoute() {
  return <ProgramEditorScreen />;
}
