import { ScanScreen } from "../../src/features/nutrition/scan/ScanScreen";

/** Full-screen AI food scan: camera → "AI is thinking" theatre → results sheet. Route: /(modals)/scan */
export default function ScanRoute() {
  return <ScanScreen />;
}
