import { RoadmapScreen } from "../../../src/features/goals/RoadmapScreen";

/** /(modals)/goal/roadmap — progress, expected vs actual, week list, recalibration. */
export default function RoadmapRoute() {
  return <RoadmapScreen />;
}
