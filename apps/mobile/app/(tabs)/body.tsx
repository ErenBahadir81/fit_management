import { PlaceholderScreen } from "../../src/ui/PlaceholderScreen";

/** F4b replaces this with `src/features/body/BodyScreen` (+ goals, weekly report). */
export default function BodyRoute() {
  return <PlaceholderScreen title="Vücut" subtitle="Ölçümler ve hedef" body="Tartı, mezura ölçümleri, hedef yol haritası ve haftalık rapor burada olacak." />;
}
