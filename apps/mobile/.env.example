# Copy to .env (never commit .env). Only EXPO_PUBLIC_* variables reach the bundle.
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1
# 1 = in-memory FakeApi (demo without a backend; login eren / eren123)
EXPO_PUBLIC_API_FAKE=0
